# 第3章 进程描述和控制

## 3.1 什么是进程

### 3.1.1 背景

### 3.1.2 进程和进程控制块

进程执行的任意时刻，都可由如下元素来表征：

- 标识符：与进程相关的唯一标识符，用来区分其他进程。
- 状态：若进程正在执行，则进程处于运行态。
- 优先级：相对于其他进程的优先顺序。
- 程序计数器：程序中即将执行的下一条指令的地址。
- 内存指针：包括程序代码和进程相关数据的指针，以及其他进程共享内存块的指针。
- 上下文数据：进程执行时处理器的寄存器中的数据。
- I/O状态信息：包括显式I/O请求、分配给进程I/O设备和被进程使用的文件列表等。
- 记账信息：包括处理器时间总和、使用的时钟数总和、时间限制、记账号等。

上述信息存放在一个称为进程控制块（process control block）的数据结构中，控制块由操作系统创建和管理。

## 3.2 进程状态

### 3.2.1 两状态进程模型

- 运行态
- 未运行态

### 3.2.2 进程的创建和终止

### 3.2.3 五状态模型

- 运行态：进程正在执行。
- 就绪态：进程做好了准备，只要有机会就会开始执行。
- 阻塞/等待态：进程在某些事件发生前不二脑功能执行，如I/O操作完成。
- 新建态：刚创建的进程，操作系统还未把它加入可执行进程组，通常是进程控制块已经创建但还未加载到内存中的新进程。
- 退出态：操作系统从可执行进程组中释放出的进程，要么它自身已停止，要么它因某种原因被取消。

### 3.2.4 被挂起的进程

#### 交换的需要

交换，把内存中某个进程的一部分或全部移到磁盘中。当内存中不存在就绪状态的进程时，操作系统就把阻塞的进程换出到磁盘中的挂起队列（suspend queue），即临时从内存中踢出的进程队列。

挂起态。

- 就绪态：进程已在内存中并可以执行。
- 阻塞态：进程已在内存中并等待一个事件。
- 阻塞/挂起态：进程已在外存中并等待一个事件。
- 就绪/挂起态：进程已在外存中，但只要载入内存就可执行。

## 3.3 进程描述

### 3.3.1 操作系统的控制结构

操作系统构造并维护其管理的每个实体的信息表。4种不同类型的表：

- 内存
- I/O
- 文件
- 进程

### 3.3.2 进程控制结构

#### 进程位置

- 属性集称为进程控制块（process control block）。
- 程序、数据、栈和属性的集合称为进程映像（process image）。

#### 进程属性

进程控制块信息分为三类：

- 进程标识信息
- 进程状态信息
- 进程控制信息

#### 进程控制块的作用

每个进程控制块都包含操作系统所需进程的所有信息。

## 3.4 进程控制

### 3.4.1 执行模式

- 非特权模式，通常称为用户模式（user mode）。
- 特权模式，称为系统模式（system mode）、控制模式（control mode）、内核模式（kernel mode）。

应用程序实现系统调用：将系统调用标识符和参数放到一个预定义的区域，然后执行一个特殊指令中断用户模式下的程序执行，将控制权交给内核。

### 3.4.2 进程创建

步骤：

- 为新进程分配一个唯一的进程标识符。此时主进程表中会添加一个新表项，每个进程一个表项。
- 为进程分配空间。
- 初始化进程控制块。
- 设置正确的链接。
- 创建或扩充其他数据结构。

### 3.4.3 进程切换

#### 何时切换进程

系统中断：

- 中断（interrupt）：与当前正在运行进程无关的某种外部事件相关，如完成一次I/O操作。
	- 时钟中断
	- I/O中断
	- 内存失效
- 陷阱（trap）：与当前正在运行进程产生的错误或异常条件相关，如非法的文件访问。

系统调用，用户进程执行系统调用时，会将用户进程置为阻塞态。

#### 模式切换

#### 进程状态的变化

## 3.5 操作系统的执行

### 3.5.1 无进程内核

### 3.5.2 在用户进程内运行

在用户进程的上下文中执行所有操作系统软件。此时操作系统是用户调用的一组例程，它在用户进程的环境内执行并实现各种功能。

当进程在内核模式下运行时，单独的内核栈用于管理调用/返回。操作系统代码和数据位于共享地址空间中，被所有用户进程共享。

发生中断、陷阱或系统调用时，处理器置于内核模式，控制权转交给操作系统。要把控制权从用户程序转交给操作系统，需要保存模式上下文并切换模式，再切换到一个操作系统例程，但此时仍然在当前的用户进程内继续执行，不需要切换进程，只是在同一进程中切换模式。

### 3.5.3 基于进程的操作系统

# 第4章 线程

## 4.1 进程和线程

### 4.1.1 多线程

### 4.1.2 线程的功能

#### 线程状态

- 运行态
- 就绪态
- 阻塞态

#### 线程同步

## 4.2 线程分类

### 4.2.1 用户级和内核级线程

线程分为两大类：

- 用户级线程（User-Level Thread，ULT）
- 内核级线程（Kernel-Level Thread，KLT），也称内核支持的线程或轻量级进程。

#### 用户级线程

管理线程的所有工作由应用程序完成，内核意识不到线程的存在。

#### 内核级线程

管理线程的所有工作均由内核完成，应用级没有线程管理代码，只有一个到内核线程设施的应用编程接口（API）。

#### 混合方法

线程创建完全在用户空间中完成，线程的调度和同步也在应用程序中进行。一个应用程序中的多个用户级线程会被映射到一些内核级线程上。

### 4.2.2 其他方案

## 4.3 多核和多线程

# 第5章 并发：互斥和同步

## 5.1 互斥：软件解决方法

软件方法通常假设在内存访问级实现了基本的互斥。

### 5.1.1 Dekker算法

#### 算法1

任何互斥方法必须基于硬件上的一些基本互斥机制。最常见的是某一时刻对某一内存地址只能有一次访问。

在这种约束下，预留一个全局内存区域，并标记为turn。进程想要进入它的临界区时，要先检查turn的内容。若turn的值等于进程号，则该进程可以进入它的临界区；否则该进程被强制等待。等待进程重复地读取turn的值，直到被允许进入临界区。这一过程称为忙等待或自旋等待，因为受阻塞的进程直到被允许进入临界区才会起实际作用。它必须循环或周期性地检查变量，因此在等待期间会耗费处理器的时间。

进程在获得临界区的访问权并完成访问后，必须为另一个进程更新turn的值。

这种解决方案保证了互斥属性，但有两个缺点：

- 进程必须严格交替使用它们的临界区，执行的步调由两个进程中较慢的进程决定。
- 若一个进程终止，则另一个进程就会被永久阻塞。

以上构造的是一种协同程序（coroutine）。协同程序能够实现程序之间执行控制权的传递。这对单个进程来说是一种有用的构造技术，但它不能充分支持并发处理。

#### 算法2

算法1的问题是要存储可以进入临界区的进程的名字，而实际上需要的是两个进程的状态。

定义一个布尔数组flag，数组中 `flag[0]`和P0关联，`flag[1]`和P1关联。每个进程能检查但不能改变另一个进程的flag值。一个进程要进入临界区，它会周期性的检查另一个进程的flag，直到其值为false，表明另一个进程不在临界区内。检查进程立即设置自己的flag为true，进入自己的临界区。离开临界区时，将自己的flag设置为false。

若一个进程在临界区外终止，包括设置flag代码时，则另一个进程不会被阻塞。然而若一个进程在临界区内终止，或在进入临界区之前已将flag设置为true，则另一个进程就会永久阻塞。

这种方案比第一种方案更差，它没有保证互斥。此方案的问题是互斥与相关进程的执行速率有关。

#### 算法3

可以通过简单交换两条语句来解决算法2中问题。

可以保证互斥。

但是，若每个进程在执行while语句之前都将flag设置为true，则每个进程都认为另一个进程已经进入临界区，引发死锁。

#### 算法4

算法3中，一个进程在设置其状态时是不知道另一个进程的状态的。由于每个进程坚持要进入临界区，导致死锁发生。

可以用一种方法解决这个问题，每个进程设置flag表明它要进入临界区，为了谦让另一个进程，会随时重设flag，为其他进程而推迟自己的请求。

但会产生活锁（livelock），死锁是指一组进程要进入临界区但没有一个进程会成功。活锁则有可能执行成功，但也又可能任何一个进程都不会进入临界区。

#### 正确的算法

Dekker算法。

### 5.1.2 Peterson算法

Dekker算法解决了互斥，但复杂的程序很难实现且其正确性也很难证明。

## 5.2 并发的原理

### 5.2.1 一个简单的例子

### 5.2.2 竞争条件

### 5.2.3 操作系统关注的问题

### 5.2.4 进程的交互

- 进程之间相互不知道对方的存在
- 进程间接知道对方的存在
- 进程直接直到对方的存在
#### 进程间的资源竞争

竞争进程面临三个控制问题：

- 互斥（mutual exclusion）
- 死锁（deadlock）
- 饥饿（starvation）

假设两个或更多进程需要访问一个不可共享的资源，这类资源称为临界资源（critical resource），使用临界资源的那部分程序称为程序的临界区（critical section）。

实施互斥产生了两个额外的控制问题，死锁和饥饿。

#### 进程间通过共享合作

#### 进程间通过通信合作

### 5.2.5 互斥的要求

## 5.3 互斥：硬件的支持

### 5.3.1 中断禁用

禁用中断可以保证互斥，但是缺点是：效率变低；不适合多处理器体系结构。

### 5.3.2 专用机器指令

一些机器指令，用于保证两个动作的原子性，如在一个取指周期中对一个存储器单元的读和写或读和测试，在这个指令执行的过程中，任何其他指令访问内存都将被阻止，而且这些动作在一个指令周期中完成。

#### 比较和交换指令

#### exchange指令

这个指令交换一个寄存器的内容和一个存储单元的内容。

#### 机器指令方法的特点

优点：

- 适用于单处理器或共享内存的多处理器上的任意数量的进程。
- 简单且易于证明。
- 可用于支持多个临界区，每个临界区可以用它自己的变量定义。

缺点：

- 使用忙等待
- 可能饥饿：当一个进程离开临界区且有多个进程正在等待时，选择哪个等待进程是任意的，因此某些进程可能会被无限的拒绝进入。
- 可能死锁

## 5.4 信号量

提供并发性的操作系统和程序设计语言的机制。

常用的并发机制：


| 机制    | 描述                                                                                                  |
| :---- | :-------------------------------------------------------------------------------------------------- |
| 信号量   | 用于进程间传递信号的一个整数值。在信号量上只可进行三种操作：初始化、递减和增加，这三种操作都是原子操作。递减操作用于阻塞一个进程，递增操作用于解除一个进程的阻塞。信号量也称为计数信号量或一般信号量。 |
| 二元信号量 | 只取0值和1值的信号量。                                                                                        |
| 互斥量   | 类似二元信号量。关键区别在于为其加锁的进程和为其解锁的进程必须为同一个进程。                                                              |
| 条件变量  | 一种数据结构，用于阻塞进程或线程，直到特定的条件为真。                                                                         |
| 管程    | 一种编程语言结构，它在一个抽象数据类型中封装了变量、访问过程和初始化代码。管程的变量只能由管程自身的访问过程访问，每次只能有一个进程在其中执行。访问过程即临界区。管程可以有一个等待进程队列。     |
| 事件标志  | 用作同步机制的一个内存字。应用程序代码可为标志中的每个位关联不同的事件。通过测试相关的一个或多个位，线程可以等待一个事件或多个事件。在全部所需位都被设定或至少一个位被设定之前，线程会一直被阻塞。   |
| 信箱/消息 | 两个进程交换信息的一种方法，也可用于同步。                                                                               |
| 自旋锁   | 一种互斥机制，进程在一个无条件循环中执行，等待锁变量的值可用。                                                                     |

基本原理：

- 两个或多个进程可以通过简单的信号进行合作，可以强迫一个进程在某个位置停止，直到它接收到一个特定的信号。
- 为了发信号，需要使用一个称为信号量的特殊变量：
	- 要通过信号量s传送信号，进程须执行原语`semSignal(s)`；
	- 要通过信号量s接收信号，进程须执行原语`semWait(s)`；
	- 若相应的信号仍未发送，则阻塞进程，直到发送完为止。

为了达到预期效果，可把信号量视为一个值为整数的变量，整数值上定义了三个操作：

- 一个信号量可以初始化成非负数。
- `semWait`操作使信号量减1。若值变成负数，则阻塞执行`semWait`的进程，否则进程继续执行。
- `semSignal`操作使信号量加1。若值小于等于零，则被`semWait`操作阻塞的进程解除阻塞。

开始时，信号量的值为零或正数：
- 若值为正数，则它等于发出`semWait`操作后可立即继续执行的进程的数量。
- 若值为零，（要么由于初始化，要么由于有等于信号量初值的进程已在等待），则发出`semWait`操作的下一个进程会被阻塞，此时该信号量的值变为负值。
- 之后，每个后续的`semWait`操作都会使信号量的负值更大。该负值等于正在等待解除阻塞的进程的数量。
- 在信号量为负值的情形下，每个`semSignal`操作都会将等待进程中的一个进程解除阻塞。

二元信号量的值只能是0或1，可由下面三个操作定义：

- 二元信号量可以初始化为0或1。
- `semWait`操作检查信号的值。若值为0，则进程执行`semWait`就会受阻。若值为1，则将值改为0，并继续执行该进程。
- `semSignal`操作检查是否有任何进程在该信号上受阻。若有进程受阻，则通过`semWait`操作受阻的进程会被唤醒；若没有进程受阻，则值设置为1。

互斥是一个编程标志位，用来获取和释放一个对象：

- 当需要的数据不能被分享或处理，进而导致在系统中其他地方不能同时执行时，互斥被设置为锁定（一般为0），用于阻塞其他程序使用数据。
- 当数据不再需要或程序运行结束时，互斥被设定为非锁定。

二元信号量和互斥量的关键区别：为互斥量加锁（设定值为0）的进程和为互斥量解锁（设置值为1）的进程必须是同一个进程。

不论是计数信号量还是二元信号量，都需要使用队列来保存在信号量上等待的进程。
- 先进先出策略，被阻塞时间最久的进程最先从队列释放，采用这一策略定义的信号量称为强信号量（strong semaphore）。
- 没有规定进程从队列中移出顺序的信号量称为弱信号量（weak semaphore）。

强信号量保证不会饥饿，弱信号量无法保证。

### 5.4.1 互斥

- 信号量的值大于等于0，表示是刻执行`semWait`而不被阻塞的进程数。这种情形允许信号量支持同步与互斥。
- 信号量小于0，该值的大小是阻塞在队列中的进程数。

### 5.4.2 生产者/消费者问题

### 5.4.3 信号量的实现

`semWait`和`semSignal`操作必须作为原子原语实现：

- 用硬件或固件实现
- 用软件方案，如Dekker算法或Peterson算法，这必然伴随着处理开销

另一种选择是：使用一种硬件支持实现互斥方案。使用比较并交换指令实现。对于单处理器系统，还可以使用禁用中断的方法。

## 5.5 管程

管程是一种程序设计语言结构，它提供的功能与信号量相同，但更易于控制。

### 5.5.1 使用信号的管程

管程是由一个或多个过程，一个初始化序列和局部数据组成的软件模块。主要特点：

- 局部数据变量只能被管程的过程访问，任何外部过程都不能访问。
- 一个进程通过调用管程的一个过程进入管程。
- 在任何时候，只能有一个进程在管程中执行，调用管程的任何其他进程都被阻塞，以等待管程可用。

管程可提供一种互斥机制：管程中的数据变量每次只能被一个进程访问。因此可以把一个共享数据结构放在管程中，从而对它进行保护。若管程中的数据代表某些资源，则管程为访问这些资源提供了互斥机制。

要进行并发处理，管程必须包含同步工具。

管程通过使用条件变量（condition variable）来支持同步，这些条件变量包含在管程中，并且只有在管程中才能被访问。有两个函数可以操作条件变量：

- `cwait(c)`：调用进程的执行在条件c上阻塞，管程选择可被另一个进程使用。
- `csignal(c)`：恢复执行在`cwait`之后因某些条件被阻塞的进程。若有多个这样的进程，选择其中一个；若没有这样的进程，什么也不做。

管程的wait和signal操作与信号量不同，若管程中的一个进程发信号，但没有在这个条件变量上等待的任务，则丢弃这个信号。

### 5.5.2 使用通知和广播的管程

Hoare管程要求产生csignal的进程必须立即退出管程或者阻塞在管程上，这种方法有两种缺陷：

- 若产生signal的进程在管程内还未结束，则需要两次额外的进程切换：阻塞这个进程需要一次切换，管程可用时恢复这个进程又需要一次切换。
- 与信号相关的进程调度必须非常可靠，产生一个csignal时，必须立即激活来自相应条件队列中的一个进程，调度程序必须确保在激活前没有其他进程进入管程，否则进程被激活的条件又会改变。

Mesa管程中，csignal原语被cnotify取代，cnotify解释如下：当一个正在管程中的进程执行cnotify(x)时，会使得x条件队列得到通知，但发信号的进程继续执行。通知的结果是在将来合适且处理器可用时恢复执行位于条件队列头的进程。但是由于不能保证在他之前有没有其他进程进入管程，因而这个等待进程必须重新检查条件。

## 5.6 消息传递

## 5.7 读者/写者问题

条件：

- 任意数量的读进程可同时读这个文件。
- 一次只有一个写进程可以写文件。
- 若一个写进程正在写文件，则禁止任何读进程文件。

### 5.7.1 读者优先

读进程具有优先权，一个读进程开始访问数据区时，只要至少有一个读进程正在读，就为读进程保留对这个数据区的控制权，因此写进程有可能处于饥饿状态。

### 5.7.2 写者优先

在一个写进程声明想写时，不允许新的读进程访问该数据区。

# 第6章 并发：死锁和饥饿

## 6.1 死锁原理

### 6.1.1 可重用资源

### 6.1.2 可消耗资源

三种处理死锁的方法：

- 死锁预防
- 死锁避免
- 死锁检测

### 6.1.3 资源分配图

### 6.1.4 死锁的条件

死锁有三个必要条件：

- 互斥
- 占有且等待
- 不可抢占

第四个条件：循环等待。


## 6.2 死锁预防

## 6.3 死锁避免

## 6.4 死锁检测

# 第7章 内存管理

# 第8章 虚拟内存

